import "./Select.css"

const Select = ({labelText}) => {
  return (
    <div className="container">
        <label>{labelText}</label>
        <select >
            
        </select>

    </div>
  )
}

export default Select